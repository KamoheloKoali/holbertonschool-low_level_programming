#include "main.h"

/**
 * _isupper - checks if an alphabet is a in caps
 * @c: variable for parameter
 * Return: exit point
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	return (0);
}

