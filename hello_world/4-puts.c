#include <stdio.h>

int main(void)
{
	/**
	 * Printing to standard output using puts()
	 * return 0 to end program if there are no erros
	 */

	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
