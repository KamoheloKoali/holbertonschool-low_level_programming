#include "main.h"
#include <stdio.h>


/**
 * main - prints program name
 * @argc: the number of arguments
 * @argv: an array of argc
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv[] _attribute_((unused));

	printf("%d\n", argc);
	return (0);
}

