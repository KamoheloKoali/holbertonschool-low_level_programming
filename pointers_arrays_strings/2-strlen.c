#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: pointer for parameter
 * Return: exit point
 */

int _strlen(char *s)
{
	char word[*s];
	int length = sizeof(word);
	return (length);
}

