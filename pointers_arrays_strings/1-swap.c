#include "main.h"

/**
 * swap_int - swaps values of two integers
 * @a: pointer for parameter one
 * @b: pointer for parameter two
 */

void swap_int(int *a, int *b)
{
	int num;
	int num1;

	num = a*;
	num1 = *b;
	*a = num1;
	*b = num;
}

